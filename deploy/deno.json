{
  "tasks": {
    "start": "deno run --allow-net --allow-read --allow-env deno_deploy.ts",
    "dev": "deno run --allow-net --allow-read --allow-env --watch deno_deploy.ts"
  },
  "compilerOptions": {
    "lib": ["deno.window"]
  }
}
